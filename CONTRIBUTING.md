# 参与开发

谢谢愿意帮忙。这个项目服务的是刚到中国、语言不通的留学生，所以有几条硬约束，请先看一眼。

## 两条不能破的底线

1. **零外部依赖**。不要引入 CDN、Google Fonts、外链图片、第三方 JS 库，也不要嵌 YouTube。
   页面必须能在国内网络、校园网、甚至完全断网的情况下双击打开。
2. **手机优先**。留学生基本只用手机看。新增内容先在 390px 宽度下确认排版没炸。

另外，所有文案**中英双语**，用 `<span class="zh">…</span><span class="en">…</span>` 的结构写，页面顶部的三态切换按钮会自动接管。

## 最常见的三种改动

### 改文案

直接改 `留学生手册-网页demo.html`。找对应的 `<span class="zh">` / `<span class="en">` 改字就行，不用懂代码。

### 改地图点位

两个办法，任选：

1. 直接编辑 `校园地图节点.js`
2. 打开 `地图标注工具.html`，可视化拖动 / 增删 / 描路，导出后替换该文件

**坐标系务必注意**（这里踩过坑）：

```js
{ id: 1, name: "西门", en: "West Gate", cat: "gate", x: 29.2, y: 46.4 }
```

- `x` 从左起算，`y` **从底边起算**，都是百分比
- 前端渲染要换算：`css_top = 100 - y`
- 上传前请用 `地图标注工具.html` 或截图确认点位落在正确的建筑上

### 修数据错误

如果发现某个点位置不对，直接开 issue（选「地图点位纠错」模板），写上地点名、当前位置、建议位置和依据（截图最好）。

## 提交

```
git clone https://github.com/SXAUICA/international-student-toolkit.git
cd international-student-toolkit
# 改完
git checkout -b fix/你的改动
git commit -m "说明改了什么"
git push origin fix/你的改动
```

然后开 Pull Request。改动小就直接合，改动大我会先问一句。

**请不要提交**：录音转写原稿、留学生个人信息（护照 / 电话 / 邮箱 / 照片）、任何能对上具体人的抱怨内容。仓库是公开的。
