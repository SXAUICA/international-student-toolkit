# 路线图：从单文件页面到网站 / 小程序 / App

## 先说三个结论

1. **现在这一版不是"临时方案"，它是内容源。** 后面无论做 H5、小程序还是 App，文案和地图数据都从这里出——所以第一件事是**把内容从界面里抽出来**，不是换框架。
2. **顺序：H5（先上线）→ 小程序 → 不考虑原生 App。**
3. **真正的门槛不是技术，是主体资质。** 小程序能不能发、网页能不能备案，取决于主体是谁，这件事要提前推，不要等代码写完才发现发不出去。

---

## 一、三个平台要付的代价（现实清单）

| 平台 | 技术难度 | 资质门槛 | 分发方式 | 建议 |
|---|---|---|---|---|
| **网页 H5** | 低（现在就有） | 国内服务器 + 域名要 ICP 备案；用境外静态托管免备案但国内访问不稳 | 链接 / 二维码，微信内可直接打开 | **先做这个** |
| **微信小程序** | 中 | 需要主体。个人主体可注册但功能受限（不能用微信支付、可选类目受限）；组织主体需要法人证件；近两年还要求完成小程序备案 | 微信内扫码 / 搜索，体验最好，学生最常打开 | **第二步** |
| **原生 App** | 高 | 苹果开发者账号年费、安卓各商店各自上架、双端维护 | 应用商店 | **不建议**，除非有明确强需求 |

> 微信的具体类目、认证与备案规则会变，落地前请以[微信官方文档](https://developers.weixin.qq.com/miniprogram/product/)为准。下面写的是判断依据，不是规则原文。

### 关于主体，这是学生社团最容易卡住的地方

学生社团一般没有营业执照。小程序的组织主体通常要**借用学校的**（找团委或学校网络信息中心），流程可能是：

1. 说明用途（校内公益、不涉及支付和用户数据交易）
2. 由学校主体注册小程序并授权你们管理
3. 同时解决备案

**这件事建议现在就并行去问**，因为它是纯行政流程，快慢不由你们决定。哪怕最后决定先做 H5，问清了也不亏。

---

## 二、技术选型

### 一套代码多端：uni-app 或 Taro

如果确定要同时做小程序和 H5（以及未来的 App），用跨端框架比写三遍划算：

| 方案 | 语言 | 能编译到 | 适合 |
|---|---|---|---|
| **uni-app** | Vue | 微信小程序 / H5 / App / 支付宝等 | 国内生态最熟，文档和例子最多 |
| **Taro** | React | 同上 | 团队熟 React 就选它 |

现在的代码是原生 HTML/CSS/JS 单文件，迁到 uni-app 相当于重写界面，但**内容不重写**——这正是第一步要先抽 JSON 的原因。

### 什么时候不该上框架

如果目标只是"让学生用手机打开一个能看的指南"，那**保持现在的单文件 HTML 就够了**，加个 PWA 就能装到桌面。框架是为了跨端，不是为了好看。别为了用框架而用框架。

---

## 三、分阶段

### Stage 0 · 内容成型（已完成）

- 双语指南页面、199 点地图数据、问题清单、接待流程、标注工具
- 全部零依赖、离线可用

### Stage 1 · 让它有个真地址（约 2 周）

目标：学生扫一个二维码就能打开，不再靠"发文件"。

- [ ] 解决托管：优先问学校要一个空间 / 子域名（**免备案、校内访问快**）
- [ ] 备选：Cloudflare Pages / Vercel 等境外静态托管（免备案，但国内访问速度和稳定性看运气，**上线前必须实测**）
- [ ] 加 PWA：`manifest.json` + Service Worker，Android 上可以「添加到主屏幕」，像 App 一样有图标、能离线打开（**已完成，见仓库根目录**）
- [ ] 生成二维码，贴到留学生公寓、食堂、公寓前台

**验收标准**：一个从没用过的学生，扫码后 3 秒内看到内容，且能自己找到"办电话卡"在哪一页。

### Stage 2 · 内容能改（约 1 个月）

目标：改一句文案不用找程序员，也不用重新发文件。

这是**现在最该做的一件事**，而且不依赖任何新平台：

- [ ] 把页面里所有 `<span class="zh">` / `<span class="en">` 抽成 `content.json`
- [ ] 地图数据已经是独立 JSON（`校园地图节点.js`），保持这样，只加字段不改结构
- [ ] 页面改成运行时读 JSON 渲染

做完之后：H5、小程序、App 可以**共用同一份内容**，改一处三端生效。

### Stage 3 · 小程序（时间取决于资质）

前置：主体 + 备案搞定。

技术上是 Stage 2 的成果换个壳：

- [ ] 用 uni-app 起项目，把 `content.json` 和地图数据接进去
- [ ] 地图用小程序原生 `map` 组件或 Canvas 重画（现在的点位数据可直接复用）
- [ ] 小程序不适合承载长文，建议只放**最常用的 5 件事** + 搜索，详细内容仍指向 H5

### Stage 4 · 可选

只有在下面两种情况才考虑做 App：需要推送通知、需要离线地图。否则小程序 + PWA 已经够用。

---

## 四、比技术更重要的事：内容维护机制

学校英文版官网 2022 年之后就没更新过——**这是这类项目最典型的死法**：上线时轰轰烈烈，半年后信息过期，学生发现不准就不再用了，然后就废了。

所以规则要提前定：

1. **每学期开学前**统一更新一次（营业时间、食堂、办事流程最容易变）
2. 更新时间写进 README，谁更新的也写上
3. 页面底部标注"最后更新：YYYY-MM"
4. 每次更新后在留学生群公告一次

内容维护比技术难，因为它需要"每年都有人"，而不是"一次有人"。

---

## 五、现在就能开始的四件事

1. **抽 JSON**（Stage 2 第一步）——纯体力活，谁都能做，做完收益最大
2. **问学校要空间 / 子域名**——行政流程慢，越早问越好
3. **并行问团委：小程序能不能挂学校主体**——同上
4. **实测境外托管的国内访问速度**——找不同网络的同学各测一次，别只听一家之言
