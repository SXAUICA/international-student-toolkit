# SXAUICA 留学生服务工具集 · 项目上下文

> 写给 AI 助手看的项目现状。给人看的说明在 `README.md`，长期路线在 `ROADMAP.md`，协作约定在 `CONTRIBUTING.md`。
> 最后更新：2026-09-13

## 这是什么

山西农业大学国际交流协会（GitHub 组织 **SXAUICA**）给在校留学生做的**离线、中英双语**校园信息工具。

仓库：https://github.com/SXAUICA/international-student-toolkit （公开，组织名下，不是个人仓库）

服务对象：刚到中国、语言不通、没有校园网账号的留学生。所有东西都要能**双击打开、断网可用、微信直接发**。

## 目录

| 文件 | 说明 |
|---|---|
| `留学生手册-网页demo.html` | 主入口，手机版双语指南 + 校园地图 + 搜索 |
| `校园地图节点.js` | **200 个校园地点数据**（中英文名 + 分类 + 坐标），地图数据源 |
| `校园地图底图.jpg` | 底图 1800×1271（原始 4016×2835 在 `D:\data structure\wasm\map-bg.jpg`） |
| `地图标注工具.html` | 可视化描点/描路工具，导出同格式数据 |
| `map_data.js` | 课设原始数据（126 点 + 115 边），保留作对照 |
| `manifest.json` / `sw.js` / `icon-*.png` | PWA，托管后「添加到主屏幕」 |
| `留学生问题清单-可解决项.md/.html/.png` | 37 条诉求分档 |
| `留学生事项落地思路.md` | 37 条收敛为 6 个载体 |
| `留学生接待流程说明.md`、`路线图.html/.png` | 新生接待流程 |

## 四条铁律

1. **零外部依赖**——不引 CDN、Google Fonts、外链图片、第三方库、YouTube 嵌入。国内网络+断网都要能用。
2. **手机优先**——390px 宽度下排版不能炸。
3. **中英双语同屏**——用 `<span class="zh">…</span><span class="en">…</span>`，页面顶部三态切换自动接管。
4. **不提交**录音转写、学生个人信息、能对上具体人的抱怨内容。仓库是公开的。

## 地图数据的关键约定（最容易踩）

`校园地图节点.js`：

```js
{ id, name, en, cat, x, y, note?, noteEn?, inset? }
```

- `x` 从左起算，**`y` 从底边起算**（两者都是百分比）。渲染时必须换算 `css_top = 100 - y`。**这个坑踩过，整张图的点上下颠倒过一次。**
- `cat` 取值：`gate` `medical` `shop` `sport` `dorm` `study` `farm` `utility` `other`
- `inset: true` = 该点在左上角「牧站总平面图」插图里，坐标已归到主图牧站区域，不是校园真实位置
- `note` / `noteEn` = 点开后显示的补充说明（可选）

网页里的**常用编号点**是另一套（在 HTML 的 `SPOTS` 数组里，20 个），和全量 200 点是两个图层。

## 当前状态（2026-09-13）

已完成：

- 双语指南页（8 个章节）、三态语言切换
- 校园地图：20 个常用编号点 + 200 个全量点 + 中英文搜索 + 分类筛选 + 全量图层开关
- 200 点数据依据底图文字标签逐个整理核对，其中 15 个插图点单独标记为 `inset`
- 地图标注工具（可视化描点/描路，导出同格式）
- PWA（manifest + service worker + 图标）
- 开源的协作文件：LICENSE、CONTRIBUTING、3 个 issue 模板、10 个 issue

未解决：

- **实验大楼、教学主楼 的职能描述被指出有误，尚未修正**（等用户给出正确用途，别自己猜）
- **保卫处**点位缺失（底图上没标）
- **留学生公寓楼号未确认**（现标在 21#–24# 公寓一带，虚线待确认）
- 199→200 个点的坐标是「文字标签位置」，未逐栋实地核验
- 道路数据完全没有（课设的 MAP_EDGES 是抽象连接，无几何）
- 底图右上角被校徽标题栏占位

## 历史教训

- 底图左上角那圈虚线**不是校园的一部分**，是「牧站总平面图 1:2500」独立插图；主图上另有「牧站区域（详见地图左上角）」标注。靠坐标查不出来，只有看图才发现。
- OCR 会把两行牌子拆成两个点（如「功能农业工程」+「研究中心」），需要合并。
- `map_data.js` 里有两个点本身就是错的：`A08 一站式服务中心` 落在标题空白区、`G4 新北门` 落在东区公寓群。
- **这个文件夹会被多人同时改**：曾经发现 HTML 里的 `SPOTS` 被他人从 11 个扩到 19 个。改大文件前先看一遍现状，别覆盖别人的改动。

## 下一步方向（用户已定）

1. **当前主线：优化校园地图**（新对话主题）
2. 中期：把文案抽成 `content.json`（issue #10），为多端做准备
3. 长期：网站 / 微信小程序 / App —— 见 `ROADMAP.md`，注意小程序的门槛是**主体资质**不是技术
